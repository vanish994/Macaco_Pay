# Vanish Pay - PIX Gateway Local

Este projeto permite gerar cobranças via PIX usando a API da GhostsPay.

## ▶️ Como rodar localmente (sem servidor)
1. Dê um duplo clique no arquivo `index.html`.
2. A página abrirá no navegador.
3. Preencha o valor e descrição, clique em "Gerar PIX".

⚠️ Se a API bloquear chamadas diretas do navegador, veja abaixo.

## 🚀 Modo alternativo com servidor (Node.js)
1. Instale Node.js: https://nodejs.org
2. No terminal, execute:

    npm install express
    node server.js

3. Acesse: http://localhost:3000

## Arquivos incluídos:
- index.html → Página principal (tema dark)
- server.js → Servidor local (opcional)
- README.txt → Este guia

